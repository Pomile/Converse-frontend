import React from 'react';

export const Icon = (props) => {
    const style = ["fal fa-angle-down"].join(' ');
    return (
        <div>
            <i className={style}></i>
        </div>
        
    );
};
